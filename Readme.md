<h1 align="center">SportTeam Manager</h1>

<h2>Описание</h2>
Консольное приложение для управления данными о спортсменах. Программа позволяет выполнять операции чтения/записи данных в различных форматах (JSON, XML, CSV, YAML), а также предоставляет функциональность для сортировки, поиска, добавления, удаления и редактирования записей.

<h2>Классы модели данных</h2>

<h3>SportTeam</h3>
Класс, представляющий сущность спортсмена.

**Свойства:**
- `int id` — уникальный идентификатор спортсмена
- `string name` — имя спортсмена
- `string secondname` — фамилия спортсмена
- `int age` — возраст спортсмена
- `string typeSport` — вид спорта
- `List<string> achivments` — список достижений

**Методы:**
- `ToString()` — возвращает строковое представление объекта
- `CompareTo(SportTeam sportTeam, int parametr)` — сравнение по указанному параметру

<h2>Работа с файлами</h2>

<h3>Интерфейс FileAction&lt;T&gt;</h3>
Базовый интерфейс для всех классов работы с файлами.

**Методы:**
- `List<T> ReadFile(string fileName)` — чтение данных из файла
- `void WriteFile(List<T> data, string fileName)` — запись данных в файл

<h3>Реализации для разных форматов</h3>

- `JsonMethods<T>` — работа с JSON файлами
- `XmlMethods<T>` — работа с XML файлами
- `YamlMethods<T>` — работа с YAML файлами
- `CsvMethods<T>` — работа с CSV файлами

<h3>Обобщенный класс generalizedMethod&lt;T&gt;</h3>
Универсальный класс для чтения и записи данных в любом поддерживаемом формате.

**Методы:**
- `List<T> ReadFile(string fileName)` — автоматически определяет формат по расширению и читает данные
- `void WriteFile(List<T> data, string fileName)` — автоматически определяет формат по расширению и записывает данные

**Особенности:**
- Автоматическое создание файла, если он не существует
- Поддержка форматов: .json, .xml, .yaml, .csv

<h2>Обработка ошибок и логирование</h2>

В программе реализована обработка исключений с использованием `try-catch` блоков и логирование через `Trace.WriteLine`:

- Проверка существования файла при чтении
- Создание файла при записи, если он отсутствует
- Обработка ошибок формата данных
- Логирование всех действий и ошибок для последующего анализа

<h2>Класс ConsoleMethods</h2>
Основной класс, содержащий бизнес-логику приложения.

<h3>Методы для работы с данными</h3>

<h4>OutputSportTeam(List&lt;SportTeam&gt; sportTeams)</h4>
Выводит данные о спортсменах на экран в удобочитаемом формате.

<h4>sortMethod(List&lt;SportTeam&gt; sportTeams, int parametr)</h4>
Выполняет сортировку списка спортсменов по указанному параметру.

**Параметры сортировки:**
- `1` — по возрасту
- `2` — по имени
- `3` — по фамилии
- `4` — по виду спорта

**Направление сортировки:**
- Пользователь выбирает направление (по возрастанию или убыванию)

<h4>SearchMethods(List&lt;SportTeam&gt; sportTeams, string stringSerach)</h4>
Выполняет поиск спортсменов по подстроке.

**Область поиска:**
- Имя (`name`)
- Фамилия (`secondname`)
- Вид спорта (`typeSport`)

**Возвращает:** отфильтрованный список спортсменов

<h4>addSTMethods(List&lt;SportTeam&gt; sportTeams)</h4>
Добавляет нового спортсмена в список. Запрашивает у пользователя:
- Имя
- Фамилию
- Возраст
- Вид спорта
- Достижения (можно добавить несколько, для завершения введите 0)

<h4>deleteSTMethods(List&lt;SportTeam&gt; sportTeams, int index)</h4>
Удаляет спортсмена из списка по индексу (номеру записи).

<h4>redactSTMethods(List&lt;SportTeam&gt; sportTeams, int index)</h4>
Редактирует данные существующего спортсмена. Можно изменить любое поле или оставить старое значение (ввод 0).

<h4>StartMethods(int task, List&lt;SportTeam&gt; sportTeams)</h4>
Центральный метод, вызывающий соответствующие функции на основе выбранного пункта меню.

<h2>Класс FileMethods</h2>
Вспомогательный класс для работы с файлами через пользовательский интерфейс.

- `ConvertFileToSportTeam(ConsoleMethods consoleMethods)` — чтение данных из файла с выбором пути через меню
- `ConvertSportTeamToFile(List&lt;SportTeam&gt; sportTeams, ConsoleMethods consoleMethods)` — запись данных в файл с выбором пути через меню

<h2>Класс MenuMethods</h2>
Класс для отображения меню и обработки пользовательского ввода.

<h3>Menu()</h3>
<h3>Главное меню программы:<h3>

```
----------------------Меню----------------------

Список доступных действий:
1) Считывание данных из файла
2) Запись данных в файл
3) Вывод данных на экран
4) Сортировка
5) Поиск
6) Добавить данные с труктуру
7) Удаление данных из структуры
8) Изменение данных из структуры
0) Выход

Ввод:
```


<h3>TypeFileMenu()</h3>
Меню выбора типа файла:
- JSON
- XML
- CSV
- YAML

<h3>SortMenu()</h3>
Меню выбора поля для сортировки.

<h3>SortDirectionMenu()</h3>
Меню выбора направления сортировки.

<h3>Класс InputsMetods</h3>
Класс для ввода данных от пользователя.

- `inputSerachString()` — ввод строки для поиска
- `inputIndex()` — ввод индекса записи
- `createSportTeam(int index)` — создание нового спортсмена через ввод данных
- `redactSportTeam(int index, SportTeam currentData)` — редактирование существующего спортсмена
- `addFileName()` — ввод имени файла
- `addType(string fileName, int TypeFile)` — добавление расширения к имени файла

<h2>Точка входа (Program.cs)</h2>

При запуске программа:
1. Пытается загрузить данные из `ProgramData.json` (если файл существует)
2. Отображает главное меню
3. Выполняет выбранные пользователем действия
4. При выходе предлагает сохранить изменения в локальный json файл `ProgramData.json`

<h2>Использование</h2>

1. Запустите приложение
2. Выберите действие из меню (введите номер пункта)
3. Следуйте инструкциям на экране
4. Для выхода в меню выберите пункт 0 или закройте окно программы